---
description: 架构决策全生命周期保障 — 读取、验证、约束、更新，防止实现偏离设计
alwaysApply: true
---

# 架构决策执行保障

AI 在讨论阶段与用户对齐的架构决策，可能在实施阶段因 subAgent 缺乏上下文、工具链障碍被绕过等原因发生偏离。此 Rule 覆盖架构决策的完整生命周期。

## 1. 新对话：读取架构决策

每轮新对话开始涉及代码实现时，先检查项目中是否存在 `ARCHITECTURE.md`（项目根目录或 `docs/` 下）。如果存在，**读取并理解其中的技术栈、架构模式和约束**，作为本轮对话的技术上下文。

## 2. 讨论产生新决策：更新文件

当讨论中产生了**框架选型、技术栈变更、架构模式调整**等决策，且用户明确确认后：

- 主动提醒："这个决策需要更新到 ARCHITECTURE.md，我来同步。"
- 更新对应章节，保留变更日期和简要原因
- 如果是推翻旧决策，在变更记录中注明原因，防止后续对话又改回去

## 3. 实施前：决策清单确认

当用户说"实施"、"开始做"、"落地"时，在写代码之前：

### 3a. 回顾已对齐的约束

- 从 `ARCHITECTURE.md` 和对话历史中提取关键技术约束

### 3b. 列出本次实施中的新决策

扫描实施计划，识别所有需要 AI 做出选择的点，按状态标注后向用户确认：

```
实施计划决策清单：
1. ✅ 已对齐：[讨论中已确认的决策]
2. ⚠️ 待确认：[AI 即将自行选择的方案] — 备选：[其他可能方案]
3. ⚠️ 待确认：[另一个需要选择的点]
请确认后开始实施。
```

**判断标准**：如果 AI 在实施中需要从两个以上可行方案中做选择，就应标为"⚠️ 待确认"。

**禁止的行为**：
- ❌ 在 thinking 中选定方案后直接实施，不在对话中体现
- ❌ 把跨服务边界的选择当作"实施细节"自行消化

## 3c. 有状态组件 Failure Mode 审查

当方案引入**有状态组件**（Redis、MQ、本地缓存、文件系统等）作为关键路径时，**必须在方案输出时附带以下审查**，不得等用户追问：

### 必须回答的问题

对方案中每个有状态组件，逐一回答：

1. **数据丢失**：该组件的数据丢失（Redis 重启/flush、MQ 未持久化）后，业务影响是什么？是否需要持久化兜底？
2. **组件不可用**：该组件宕机时，服务是否可降级？还是直接不可用？
3. **重启恢复**：服务重启后，如何恢复到正确状态？是否依赖组件中的数据仍然存在？
4. **并发安全**：多实例同时操作该组件时，是否有竞态条件？

### 严重性分级

不同场景对组件可靠性的要求不同，AI 必须区分：

- **可容忍丢失**：缓存、限流计数器、Token 黑名单（用户重新登录即可）
- **不可容忍丢失**：ID 计数器（导致重复）、交易状态（导致资金错误）、用户数据（导致丢失）

对"不可容忍丢失"的场景，方案中**必须包含持久化/恢复机制**，不得只依赖内存/Redis。

### 禁止的行为

- ❌ 方案中使用 Redis 存储关键状态，但不讨论丢失后的恢复策略
- ❌ 把"Redis 是否可靠"当作已知前提跳过，不显式评估
- ❌ 用"后续可以加"来推迟关键路径上的持久化设计

### 历史教训

> 在此记录本项目中因未审查有状态组件 failure mode 而导致返工的案例。

## 4. 派发 subAgent：携带技术栈上下文

向 subAgent 发送任务时，prompt 中**必须包含**：

- ✅ 框架和版本（如 "基于 go-kratos/kratos/v2"）
- ✅ 核心库约束（如 "HTTP Server 用 kratos transport/http，禁止 gorilla/mux"）
- ✅ 分层约定（如 "server → service → biz ← data"）
- ✅ 禁止事项（如 "不引入计划外的框架级依赖"）

❌ 禁止只给功能描述不给技术约束

## 5. 遇到障碍：上报而非绕过

以下情况**必须停下来让用户决定**，不能自行绕过：

- 计划中的依赖/工具安装失败
- 技术方案在实现中遇到困难
- 需要引入计划外的框架级依赖
- 需要改变已对齐的架构模式

## 6. 范围缩减须上报

当 AI 判断某个**已对齐的计划项**"可以先不做"、"可以简化实施"或"留到后续迭代"时，**必须在动手前**明确告知用户：

- 📋 原计划是什么
- ✂️ 打算简化/跳过什么
- 💡 为什么要简化（复杂度、风险、时间）
- ⚠️ 简化后的已知取舍（技术债、workaround、功能缺失）

**用户确认后才能执行简化版。**

### 典型触发场景

- TODO 中写了 A+B，实施时觉得"先只做 A 就够了"
- 讨论中提到的能力（如 proto 定义 config），实施时用了更简单的替代方案
- 某一步增加了预期外的复杂度，想跳过或降级处理

### 禁止的行为

- ❌ 内部判断"这步可以省略"后直接执行，不通知用户
- ❌ 用 workaround 解决了简化带来的副作用，但不说明这是因为跳过了原计划
- ❌ 在思考过程中决定简化，但在对话输出中不体现

## 7. 实施后：一致性自检

一轮实施完成后快速检查：

- 新代码的依赖是否与 `ARCHITECTURE.md` 一致？
- 是否有计划中应使用但实际未使用的框架组件？
- 如有偏离，是否已获得用户确认？

## 8. 重复模式检测

### 实施过程中

当一轮实施**修改了 3 个以上同类文件的相同模式**时（如同时在多个文件中添加相同中间件、在多个数据层文件中添加相同连接逻辑），**必须在完成后主动提醒用户**：

> "本次修改了 N 个文件的同一模式（[模式描述]），是否需要提取为共享抽象？"

### 创建新模块/服务时

创建新模块时，**必须先检查 ARCHITECTURE.md 的共享抽象章节**，使用已有的共享包，不得复制已有模块的代码然后修改。

### 根因

跨对话的增量开发中，AI 会将已有代码中的重复当作「约定」来遵守，而非当作「坏味道」来质疑。此规则确保在重复累积到阈值时触发反思。

## 9. 微架构决策边界

AI 容易对"大决策"（换框架、加依赖）保持警觉，但对"小决策"判断失误，把跨边界选择当实施细节自行消化。以下清单定义**必须讨论的微架构决策**，即使它们看起来很小。

### 必须讨论（影响跨模块/跨服务边界）

以下变更**无论多小，都必须先与用户讨论方案**：

- 新增/修改 API 契约字段（proto、OpenAPI schema 等）
  - ⚠️ 新增字段时必须按 `api-biz-design-guidelines` 规则检查字段类型（enum vs string），不可默认用 string
- 改变数据在模块/服务间的传播方式（API 字段 vs 中间件 metadata vs context）
- 修改共享抽象的导出类型（公共 struct、interface、error type 等）
- 新增共享存储的 key 命名模式或存储结构
- 改变错误码体系或响应结构

### 可自行决定（模块内部实现）

以下变更可在实施中自行决定，无需逐一确认：

- 本地变量/函数命名
- 模块内部的代码组织（拆分文件、提取私有函数）
- 日志内容和级别
- 单元测试的断言方式

### 灰色地带：默认上报

如果 AI 无法确定某个决策属于"必须讨论"还是"可自行决定"，**默认上报**。宁可多问一次，不可擅自决定后被纠偏。

### 历史教训

> 在此记录本项目中因未讨论微架构决策而导致返工的案例，帮助 AI 校准判断。

## 10. 关联规则

以下专项规则在特定场景下被本规则引用，AI 在相关场景下应同时遵守：

- **`api-biz-design-guidelines`**：Proto 字段类型、Enum 设计、Biz 层签名与返回值规范。在新增/修改 proto 字段或 biz 方法时触发。
- 其他项目特有的专项规则可在此补充。
